name: Release Workflow

on:
  push:
    branches: [development, release-*]

jobs:
  release:
    uses: epam/ai-dial-ci/.github/workflows/python_docker_release.yml@2.6.2
    secrets: inherit
    with:
      platforms: "linux/amd64,linux/arm64"