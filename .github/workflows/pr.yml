name: PR Workflow

on:
  pull_request:
    branches: [development, release-*]

jobs:
  run_tests:
    uses: epam/ai-dial-ci/.github/workflows/python_docker_pr.yml@2.7.2
    secrets: inherit
    with:
      platforms: "linux/amd64,linux/arm64"